#include "monty.h"
/**
 * nop - doesn't do anything
 * @stack: pointer to the first node of the stack
 * @line_number: line of the current operation
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
